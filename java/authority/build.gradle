plugins {
   id 'java'
}

sourceCompatibility = '1.8'

configurations {
   api
   compile.extendsFrom(api)
}

repositories {
   mavenLocal()
   mavenCentral()
}

dependencies {
   // modules
   api project(':java:models')
   api project(':java:interfaces')
}
